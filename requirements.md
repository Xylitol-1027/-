# 家計簿アプリ 要件定義書

作成日: 2026-07-21

## 1. 概要

スマホ（Android）で使う家計簿アプリ。同棲パートナーとの「折半」「立替」の精算計算と、支出が生活に必要か浪費かを判定して節約に役立てることを主目的とする。レシート読み取りによる入力の手間削減も重視する。

## 2. 開発方針

- **開発方式**: Flutter（Dart）によるクロスプラットフォーム開発。まずAndroidをターゲットに実装する。
- **動作形態**: MVPは単独端末で完結させる。ログインや複数端末間のリアルタイム共有は行わない。同棲相手の支出も、アプリ操作者本人が代理で入力する運用を前提とする。
- **データ保全**: 同期機能がないことによる機種変更・故障・紛失時のデータ消失リスクに備え、Android標準の自動バックアップ機構、またはGoogleドライブへの書き出し機能をMVPに含める。ログイン（アカウント認証）は不要な方式とする。

## 3. レシート読み取り機能

### 3.1 OCRエンジン

- Google ML Kit（オンデバイスOCR）を採用する。無料でオフライン動作が可能。

### 3.2 処理の2層構成

1. **OCR層**: レシート画像から文字とその位置情報（バウンディングボックス）を取得する。
2. **振り分けロジック層（自作パーサー）**: OCR結果を受け取り、以下のようなルールベースで「日付」「支払先」「金額」の候補を判定する。
   - 日付: `YYYY/MM/DD` `YYYY年MM月DD日` 等の正規表現パターンに一致する文字列を抽出
   - 支払先: レシート上部（バウンディングボックスのY座標が最小の範囲）にある文字列を候補とする
   - 金額: 「合計」「お会計」「小計」等のキーワード近傍の数値、またはレシート内最大金額を候補とする
   - 判定結果はユーザーが確認・修正できるようにする

この2層構成により、将来的に精度が不足する場合はOCR層のみをクラウドの明細解析API（Google Document AI Expense Parser等）に差し替えることが可能。ただしクラウドAPIは通信必須・従量課金となるため、MVPでは採用しない。

### 3.3 読み取り粒度

- レシート1枚 = 1取引として記録する。
- 商品ごとの明細行分割（1レシートを複数カテゴリの取引に分ける）は行わない。カテゴリ・必要度判定もレシート全体に対して1つ設定する。

## 4. 入力項目

| 項目 | 内容 |
|---|---|
| 日付 | 取引日 |
| 支払先 | 自由入力＋過去入力履歴からのオートコンプリート候補表示。固定リストでの事前登録制は採らない |
| カテゴリ | 初期リストを用意し、ユーザーが追加・編集・削除可能（詳細は5章） |
| 金額 | 円単位 |
| 折半/立替/空白フラグ | 詳細は6章 |
| 支払者 | 折半・立替・空白のいずれを選んだ場合も常に選択する。表示名は設定画面で自由に変更できる（詳細は6.2節） |
| 必要/浪費フラグ | カテゴリのデフォルト値を自動適用し、取引ごとに上書き可能（詳細は5章） |

入力方法はレシートOCRのほか、手入力にも対応する。登録済み取引の編集・削除にも対応する。

## 5. カテゴリと必要度（浪費）判定

### 5.1 カテゴリ

- 初期カテゴリリスト（食費・日用品・交通費・娯楽費・交際費・美容・通信費・住居費 等）を用意する。
- ユーザーはカテゴリを自由に追加・編集・削除できる。
- カテゴリの並び順はユーザーが自由に並び替えられる（カテゴリ管理画面でドラッグ&ドロップ操作）。この並び順は、取引の登録・編集フォームのカテゴリ選択にも反映する。月次サマリーのカテゴリ別内訳（支出金額の多い順で表示）には適用しない。

### 5.2 必要/浪費判定（節約用）

- カテゴリごとに「必要」「浪費」のデフォルト値を設定できる（例: 食費・日用品・住居費・通信費→必要、娯楽費・交際費・嗜好品→浪費）。
- 取引登録時はカテゴリのデフォルト値が自動適用される。個別の取引ごとに手動で上書き可能（例: 食費カテゴリだが今回は贅沢したデザート代なので「浪費」に変更）。
- 予算上限設定・超過アラート機能は本要件のスコープ外とする（実績の振り返り表示のみ。7章参照）。

## 6. 折半・立替の計算ロジック（同棲用）

### 6.1 フラグの意味

- **折半**: 支払者が全額を立て替え、金額の1/2を非支払者の負債として計上する。
- **立替**: 支払者が全額を立て替え、金額の全額(1/1)を非支払者の負債として計上する（一方が他方の分を完全に肩代わりする取引）。
- **空白**: 個人の支出として記録するのみで、精算対象の負債には計上しない。

### 6.2 支払者

- 折半・立替・空白のいずれを選んでも、支払者を選択する項目を常に表示する。
- 空白の場合の支払者は記録用（どちらの財布から出た支出かを残すため）であり、精算の負債計算（6.1節）には使用しない。負債計算に使うのは折半・立替の場合のみ。
- 「自分」「相手」という表示名は固定ではなく、設定画面で自由な名前（例: 実際の名前やニックネーム）に変更できる。変更した名前は支出一覧・登録フォーム・精算画面などアプリ全体の表示に反映される。
- 折半/立替/空白フラグは、新規登録時にどれを最初から選んだ状態にするか（既定値）を設定画面で指定できる。多くの支出が折半であれば「折半」を既定にする、といった使い方を想定する。登録の都度、既定値から変更することも可能。

### 6.3 端数処理

- 折半で割り切れない金額（奇数円）が発生した場合は、1円単位の四捨五入で処理する。切り捨て/切り上げの厳密なルールは設けない。

### 6.4 精算

- 「精算する」操作により、その時点までの折半・立替の負債合計をゼロにリセットする（精算履歴として記録が残る）。
- 月次での自動リセットは行わない。精算のタイミングはユーザーが任意に決める。

## 7. 分析・レポート機能

- サマリー画面を用意し、「月次」「年次」を切り替えて集計できるようにする。
  - カテゴリ別の支出割合を円グラフ（ドーナツ型）で表示し、一目で内訳が分かるようにする。カテゴリごとの金額・比率は凡例（リスト）でも確認できる。
  - 「必要/浪費」の合計金額・比率も表示する。
- 予算設定・超過アラート機能はMVPには含めない（実績の振り返りのみ）。

## 8. 編集・削除

- 登録済みの取引は編集・削除に対応する。
- 削除時は確認ダイアログを表示する。削除は完全削除とし、ソフトデリート（ゴミ箱・復元機能）は設けない。

## 9. スコープ外（次フェーズ以降）

以下は「できたらほしい」機能として要望はあるが、MVPには含めない。

- 同期機能（ログイン、複数端末間でのクラウド同期）
- 共有機能（同棲相手と同じ帳簿をリアルタイムに共同編集）
- カテゴリ別予算上限設定・超過アラート
- レシート明細行単位でのOCR解析・複数取引への自動分割

これらは、まず単独動作のMVPを実際に使用し、ニーズを検証した上で導入を検討する。
